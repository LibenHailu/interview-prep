#include <iostream>
int main()
{

    // auto x = 3;
    // auto y = 39;
    // auto a = x + y;
    // std::cout << ++y + x + x;
    // auto x = 85;
    // auto y = std::to_string(25);
    // auto a = std::stoi(std::to_string(++x) + y);
    // std::cout << y << ++x;
    // auto x = 42;
    // auto y = 89;
    // auto a = x + y;
    // std::cout << ++y + x + x;
    // auto x = 4;
    // auto y = std::to_string(4);
    // auto a = std::stoi(std::to_string(++x) + y);
    // std::cout << y << ++x;
    auto x = 60;
    auto y = std::to_string(76);
    auto a = std::stoi(std::to_string(++x) + y);
    // std::cout << y << ++x;
    std::cout << a ;
}